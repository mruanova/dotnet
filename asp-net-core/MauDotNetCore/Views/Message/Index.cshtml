@{
    ViewData["Title"] = "Message Page";
}

<partial name="NavPartial"/>

@model List<Message>
@foreach(var message in Model)
{
    <p>@message.Creator.FirstName @message.Creator.LastName says: @message.Content</p>
}